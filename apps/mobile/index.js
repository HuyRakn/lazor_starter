// CRITICAL: Import polyfills FIRST before anything else
// This ensures window and localStorage are available before any modules are loaded
import './src/polyfills';

// Now import expo-router entry
import 'expo-router/entry';

