/**
 * ThreeDMarquee component props
 */
export interface ThreeDMarqueeProps {
  /**
   * Array of image URLs to display in the marquee
   */
  images: string[];
  /**
   * Optional className for container
   */
  className?: string;
  /**
   * Optional height override
   */
  height?: string;
}

